type Card {
  NewField: String
  value: String
  type: CardType
  flipped: Boolean
}

type Board @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  cards: [Card]
  hasEnded: Boolean
}

enum CardType {
  ID
  BLUE
  RED
  NEUTRAL
  DEATH
}
